# Configurações de Segurança e Ambiente
# NÃO modifique este arquivo com segredos reais. Copie para .env e edite lá.

# Chave secreta para assinatura de sessões (Gere uma chave forte em produção)
# Ex: output de `python -c 'import secrets; print(secrets.token_hex(32))'`
FLASK_SECRET_KEY=dev_key_insegura_apenas_para_exemplo

# Ambiente (development, production, testing)
FLASK_ENV=development

# Porta do servidor (padrão 5000)
PORT=5000

# Caminho para o arquivo de parâmetros (Opcional, padrão é config/beir_hotspot_parameters.json)
# PARAMS_FILE=config/beir_hotspot_parameters.json
